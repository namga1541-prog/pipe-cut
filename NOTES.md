# 자동용접팀 배관 제단 계산기 — 작업 노트

> 앱 이름: 2026-06-11 "광표반장님 …" → "자동용접팀 배관 제단 계산기"로 변경. UI는 라이트 테마(흰 배경).

> PC 간(회사 ↔ 집) 작업을 이어가기 위한 맥락 메모. 코드는 git, 맥락은 이 파일로 같이 push 한다.

## 무엇인가
SUS 배관을 6m 원자재에서 **짜투리 최소 + 본수 최소**로 잘라내는 최적화 PWA 계산기.
자동용접팀 현장용. 모바일 우선 단일 HTML 앱(빌드 도구 없음). 라이트 테마.

## 파일 구조
| 파일 | 역할 |
|------|------|
| `index.html` | 앱 본체 (UI + 최적화 솔버) |
| `manifest.json` | PWA 매니페스트 (홈화면 설치) |
| `sw.js` | 서비스워커 (오프라인 + 자동 최신화) |
| `icon.svg` | 앱 아이콘 |

## 핵심 로직 (index.html)
- `solve(cfg)`: 최적화 진입점. 1순위 **신규 본수** 최소, 2순위 신규본 최대 자투리(재사용성).
- `packBins(pieces, freshCap, leftCaps)`: Best-Fit Decreasing. **보유 자투리 빈을 먼저 깔고 신규 본보다 우선 채움**(rank: left=0, fresh=1).
- `shuffle(a, seed)`: 결정적 의사난수. `solve`에서 2500회 무작위 재시작.
- 모델링: 각 조각 소요 = 길이 + kerf(톱날). 신규 한 본 사용가능 = 6000 − 끝단손실(기본 100mm). **자투리는 끝단손실 없음**(이미 절단면), kerf만 적용.
- 기본 예시값(미입력 시): 3200×1, 3100×2, 1500×6, 1300×5, 1250×7.

## 구현된 기능 (2026-06-11 추가)
1. **자투리 재고 풀**: "♻️ 보유 자투리" 카드에 창고 잔재 입력 → 신규 본 자르기 전 먼저 소진. 결과의 "♻ 재사용 가능 자투리"를 `재고에 추가` 버튼으로 다음 작업 잔재로 누적.
2. **현장 작업지시서**: 본별 체크박스(작업 중 ✓) + 컷 마킹 누적 위치(`📍 절단 위치: 1500·3000·…`) 표시. 인쇄/PDF에 포함.
3. **카톡 공유**: 입력값을 URL `#d=`에 인코딩. `navigator.share`(모바일 공유시트→카톡) / 클립보드 폴백. 링크 열면 입력 자동 복원(공유링크 > 저장값 > 예시 순).
- 막대는 원자재 실길이에 비례(자투리 본은 짧게 표시). 요약 3지표: 신규 본수 / 잔재 활용(개) / 버림 짜투리(mm).

## 모바일 최적화 (2026-06-11, 사용 90%가 모바일)
- 입력 필드 높이 46px(터치), 숫자 스피너 제거, iOS 줌 방지(폰트 16px 유지), `text-size-adjust:100%`.
- 계산 버튼: 입력 중 화면 하단 **sticky 고정**(엄지 도달) → 결과 나오면 `body.has-result`로 static 전환(결과 안 가림).
- 버튼 `touch-action:manipulation`(더블탭 줌·300ms 지연 제거), body `overscroll-behavior-y:contain`(당겨서 새로고침 방지).
- 막대 세그먼트: 폭 9% 미만 조각은 숫자 라벨 생략(가독성) — 단 아래 ✂ 목록엔 전부 표기.
- 계산 시 입력값 **자동 저장**(실수 새로고침 대비). 안전영역(safe-area-inset) 반영.

## 코딩 컨벤션
- 바닐라 JS, `var`/`function` 일관 (arrow·let·const 미사용 — MADI와 동일 스타일).
- localStorage 키: `pipeCutCalc.v1`.

## 배포
- 정적 호스팅(GitHub Pages 등). `main` push가 곧 배포.
- sw.js 캐시 버전 갱신 시 강제 새로고침 필요.

## PC 간 작업 규칙
- 작업 시작: `git pull` 먼저.
- 작업 끝: `git add -A && git commit && git push origin main`.
- 맥락이 바뀌면 이 NOTES.md도 같이 갱신해서 push (메모리는 PC별 로컬이라 동기화 안 됨).

## 향후 개선 후보 (미착수)
- 구경/스케줄(20A~200A · Sch 10~40) 선택값이 현재 계산엔 미반영 — 라벨 표시용. 두께별 **중량(kg) 산출** 여지.
- **단가 연동** → 총 자재비 + 버려지는 짜투리 금액 표시.
- 여러 사양을 한 번에 계산(현재는 단일 사양 기준).
- ~~자투리 재고 풀~~ → 2026-06-11 구현 완료.
