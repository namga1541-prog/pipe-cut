# 광표반장님 배관 제단 계산기 — 작업 노트

> PC 간(회사 ↔ 집) 작업을 이어가기 위한 맥락 메모. 코드는 git, 맥락은 이 파일로 같이 push 한다.

## 무엇인가
SUS 배관을 6m 원자재에서 **짜투리 최소 + 본수 최소**로 잘라내는 최적화 PWA 계산기.
광표반장님 현장용. 모바일 우선 단일 HTML 앱(빌드 도구 없음).

## 파일 구조
| 파일 | 역할 |
|------|------|
| `index.html` | 앱 본체 (UI + 최적화 솔버) |
| `manifest.json` | PWA 매니페스트 (홈화면 설치) |
| `sw.js` | 서비스워커 (오프라인 + 자동 최신화) |
| `icon.svg` | 앱 아이콘 |

## 핵심 로직 (index.html)
- `solve(cfg)` (≈L301): 최적화 진입점. 1순위 본수 최소, 2순위 최대 단일 자투리(재사용성).
- `packBins()` (≈L274): Best-Fit Decreasing 빈 패킹.
- `shuffle(a, seed)` (≈L290): 결정적 의사난수. `solve`에서 2500회 무작위 재시작으로 더 나은 해 탐색.
- 모델링: 각 조각 소요 = 길이 + kerf(톱날). 한 본 사용가능 = 6000 − 끝단손실(기본 100mm).
- 기본 예시값(미입력 시): 3200×1, 3100×2, 1500×6, 1300×5, 1250×7.

## 코딩 컨벤션
- 바닐라 JS, `var`/`function` 일관 (arrow·let·const 미사용 — MADI와 동일 스타일).
- localStorage 키: `pipeCutCalc.v1`.

## 배포
- 정적 호스팅(GitHub Pages 등). `main` push가 곧 배포.
- sw.js 캐시 버전 갱신 시 강제 새로고침 필요.

## PC 간 작업 규칙
- 작업 시작: `git pull` 먼저.
- 작업 끝: `git add -A && git commit && git push origin main`.
- 맥락이 바뀌면 이 NOTES.md도 같이 갱신해서 push (메모리는 PC별 로컬이라 동기화 안 됨).

## 향후 개선 후보 (미착수)
- 구경/스케줄(20A~200A · Sch 10~40) 선택값이 현재 계산엔 미반영 — 라벨 표시용. 두께별 무게/단가 연동 여지.
- 여러 사양을 한 번에 계산(현재는 단일 사양 기준).
- 자투리 재고를 다음 계산에 이월(잔재 풀) 기능.
